# Сервисный токен вашего прокси — клиенты должны прислать его в Authorization: Bearer <...>
SERVICE_TOKEN=replace-with-strong-random-token

# Ключ OpenAI, которым прокси будет подписывать исходящие запросы
OPENAI_API_KEY=sk-...

# Опционально — адрес и порт бинда
BIND_HOST=0.0.0.0
BIND_PORT=8567

# Логи: RUST_LOG="debug" для подробностей
RUST_LOG=info
